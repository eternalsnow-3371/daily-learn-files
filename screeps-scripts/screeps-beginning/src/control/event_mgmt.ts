/* eslint-disable */
/* still work on it. */

import { Zone } from "leviathan/zone";

export class EventMgmt {
    private zone: Zone;
    private zoneName: string;

    public registerEvent(event: string): void {
        // to be done.
    }

    private hooker(event: string): void {
        // do nothing.
    }
}
